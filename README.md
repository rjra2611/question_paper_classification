# question_paper_classification

The project is an attempt to classify question papers into their respective categories.

It uses google Ocr Tesseract to read data and gives it to unsupervised classifier.
